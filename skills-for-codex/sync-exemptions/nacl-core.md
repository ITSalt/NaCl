Source root: fix/validation-skills-v2.15-integration (skill routing table: nacl-sa-validate "L1-L6" → "L1-L13"); feat/2.24.0-desktop-graph-runtime (graph-doctor.mjs pointer, worktree-safe Project Root Resolution, canonical Graph-Down HALT block, Concurrent Sessions section)
Intentional divergence: The Codex variant carries no skill-routing table with per-skill validation-level spans — it covers shared conventions (IDs, schema, Excalidraw) only, and its ID/label sections already include the 2.15 extension labels. The stale "L1-L6" text existed in the root file only. The 2.24.0 additions are all Claude Code Desktop/CLI execution mechanics that have no Codex counterpart: (1) `graph-doctor.mjs --fix` assumes Bash-capable remediation of a local/sidecar Neo4j — nacl-core (Codex) deliberately treats graph access as a conditional capability with "Connection strings are for user-facing diagnostics only" and no assumed shell/Docker access, so it has nothing to point the script from; (2) the `$NACL_PROJECT_ROOT` worktree-safe resolution and the whole "Concurrent Sessions" section describe the specific Claude Code Desktop topology of parallel sessions running in linked git worktrees off one main checkout — the Codex variant's session/workspace model is not documented anywhere as having this topology, so there is nothing to adapt without inventing Codex runtime behaviour; (3) the canonical Graph-Down HALT block is the same abstraction point already exempted per-skill (see nacl-ba-process.md et al.) — Codex reports graph unavailability via the closed `BLOCKED` vocabulary, not an actionable docker/graph-doctor recipe.
Next review: next NaCl release that gives the Codex channel either a documented multi-session/worktree topology or a scripted graph-remediation path (graph-doctor.mjs shipped through skills-for-codex + Bash-capable Codex hosts documented)
