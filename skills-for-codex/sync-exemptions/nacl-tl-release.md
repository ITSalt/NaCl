Source root: feat/skill-tools-pilot (Step-3 CI watch + health probe extracted into scripts/wait-for-ci.sh and scripts/health-check.sh); earlier: fix/skill-branch-literals (`main` → `{main_branch}`)
Intentional divergence: The Codex variant is a condensed, delegating version that reads `../nacl-tl-core/references/*` and reproduces neither (a) the changelog-freshness cross-check git block (`gh pr list … --base main …`) nor (b) the Step-3a `gh run watch` / Step-3b `curl` health-probe mechanics now owned by the bundled helpers `scripts/wait-for-ci.sh` and `scripts/health-check.sh` (root-side single authorities, pinned by their `*.test.sh`). Those scripts are not part of the Codex distribution, so the root delegation has no Codex counterpart to mirror.
Next review: next NaCl release that touches nacl-tl-release
