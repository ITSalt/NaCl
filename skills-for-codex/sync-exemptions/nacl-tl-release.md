Source root: fix/release-gate-fix-prs (Step-2 pre-merge gate became type-aware: fix PRs verified via Decision node / `Fix-level` marker through the bundled classifier `nacl-core/scripts/classify-pr-merge.mjs`, not a Task node); earlier: feat/skill-tools-pilot (Step-3 CI watch + health probe extracted into scripts/wait-for-ci.sh and scripts/health-check.sh); earlier: fix/skill-branch-literals (`main` → `{main_branch}`)
Intentional divergence: The Codex variant is a condensed, delegating version that reads `../nacl-tl-core/references/*` and reproduces neither (a) the changelog-freshness cross-check git block (`gh pr list … --base main …`), (b) the Step-3a `gh run watch` / Step-3b `curl` health-probe mechanics now owned by the bundled helpers `scripts/wait-for-ci.sh` and `scripts/health-check.sh` (root-side single authorities, pinned by their `*.test.sh`), nor (c) the type-aware Step-2 pre-merge graph-proof gate (feature PRs → Task node; fix PRs → accepted Decision node, or an L0/L1 code-only `Fix-level` marker — computed by `nacl-core/scripts/classify-pr-merge.mjs`, pinned by its `.test.mjs`). The condensed variant models release gating only through the W4 Strict-Only block conditions and does not reproduce the per-PR Step-2 gate. Those scripts are not part of the Codex distribution, so the root delegation has no Codex counterpart to mirror.
Next review: 2.20.0
