Source root: feat/skill-tools-pilot (Phase-2 wave assignment extracted into nacl-tl-plan/scripts/wave-plan.mjs; extended with an `assign` mode + `waveStart` offset so the incremental `--feature` path also routes through the tool)
Intentional divergence: The Codex variant describes wave planning at the principle level ("build an execution wave plan from dependencies and priorities"; "wave planning by dependencies and priority") and intentionally does not reproduce the procedural numbering scheme (`plan` mode: `depth`/`1 + 2·depth`/BE-FE formula) nor the incremental `assign` mode (`waveStart + topological-level` over an explicit task DAG). The bundled helper `scripts/wave-plan.mjs` (root-side single authority, pinned by `scripts/wave-plan.test.mjs`) is not part of the Codex distribution, so the root delegation has no Codex counterpart to mirror.
Next review: next NaCl release that touches nacl-tl-plan
