Source root: feat/skill-tools-shared-core (Step-3 CI watch + health probe delegated to nacl-core/scripts/{wait-for-ci,health-check}.sh); earlier: fix/skill-branch-literals (`main` → `{main_branch}`)
Intentional divergence: The Codex variant is a condensed, delegating version that reads `../nacl-tl-core/references/*` and reproduces neither (a) the delivery pre-check git-command block (`git log main …`) where the `main` → `{main_branch}` fix applied nor (b) the Step-3 `gh run watch` / `curl` health mechanics now owned by the shared helpers nacl-core/scripts/wait-for-ci.sh + nacl-core/scripts/health-check.sh (pinned by their `*.test.sh`). None are part of the Codex distribution, so the root delegation has no Codex counterpart to mirror.
Next review: next NaCl release that touches nacl-tl-deliver
