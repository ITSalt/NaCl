Source root: 2.22.0 cross-platform-graph-init (Step 2c rewritten to dispatch to per-OS setup scripts nacl-tl-core/scripts/setup-graph.{ps1,sh}: official neo4j-mcp binary resolution, BOM-free .mcp.json pointed directly at the binary, and a hard healthy+constraints+handshake gate); feat/2.24.0-desktop-graph-runtime (new 2c.-1 worktree guard HALT before 2c.0; 2c.4 success block reworded for Desktop `/mcp` vs CLI restart with a post-reload `RETURN 1` verification call; failed-check table gains docker-cli-missing / docker-daemon-down rows and a resolve-binary manual-download fallback line).
Intentional divergence: The Codex variant delegates graph work to nacl-init-project.sh, which only *creates* graph-infra files (byte-preserving cp of the clean schema, no BOM) and neither writes .mcp.json nor resolves/starts the neo4j-mcp server — MCP wiring is configured through Codex's own runtime, not by the skill. The launcher/BOM/STDOUT-banner failure modes the root Step 2c now fixes do not arise on the Codex path, so the per-OS setup-graph scripts and the .mcp.json/handshake gate have no Codex counterpart to mirror. The 2.24.0 additions are the same Step 2c surface and inherit the same reasoning: (1) the 2c.-1 worktree guard describes the Claude Code Desktop topology of parallel sessions in linked git worktrees off one main checkout — the Codex variant's Invocation Semantics model sessions/`/clear` differently and document no such worktree topology, so there is nothing to mirror without inventing Codex runtime behaviour; (2) the reworded MCP-restart success block (`/mcp`, `claude` CLI restart, `mcp__neo4j__read-cypher` verification call) names Claude-specific commands and tool names that do not exist in Codex, where MCP wiring is "configured through Codex's own runtime, not by the skill" per the original divergence; (3) docker-cli-missing/docker-daemon-down and the resolve-binary fallback are failure modes of the root's own binary-resolution + Docker-start flow (setup-graph.{ps1,sh}), which nacl-init-project.sh never runs on the Codex path.
Next review: next NaCl release that changes how the Codex runner provisions graph infrastructure, MCP wiring, or documents a Codex multi-session/worktree topology.
