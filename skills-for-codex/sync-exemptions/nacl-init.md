Source root: 2.22.0 cross-platform-graph-init (Step 2c rewritten to dispatch to per-OS setup scripts nacl-tl-core/scripts/setup-graph.{ps1,sh}: official neo4j-mcp binary resolution, BOM-free .mcp.json pointed directly at the binary, and a hard healthy+constraints+handshake gate).
Intentional divergence: The Codex variant delegates graph work to nacl-init-project.sh, which only *creates* graph-infra files (byte-preserving cp of the clean schema, no BOM) and neither writes .mcp.json nor resolves/starts the neo4j-mcp server — MCP wiring is configured through Codex's own runtime, not by the skill. The launcher/BOM/STDOUT-banner failure modes the root Step 2c now fixes do not arise on the Codex path, so the per-OS setup-graph scripts and the .mcp.json/handshake gate have no Codex counterpart to mirror.
Next review: next NaCl release that changes how the Codex runner provisions graph infrastructure or MCP wiring.
