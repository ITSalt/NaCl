Source root: feat/2.24.0-desktop-graph-runtime (Graph-Down HALT compacted to a one-liner offering `node nacl-core/scripts/graph-doctor.mjs --fix`, Docker Desktop / local compose, and sidecar relaunch, replacing the old "Neo4j is not reachable... ensure Docker is running" table cell / ERROR string).
Intentional divergence: The Codex variant carries no equivalent table cell or ERROR string at all — graph unavailability is reported through the closed `BLOCKED` verification vocabulary (see nacl-core.md "Blocked Or Unverified Reporting"), not an actionable remediation script. The root canon assumes a Bash-capable host that can run `graph-doctor.mjs --fix` or `docker compose up -d` directly; nacl-core (Codex) treats graph access as a conditional capability and explicitly limits itself to "Connection strings are for user-facing diagnostics only" with no assumed shell/Docker access. There is no per-skill HALT text to mirror because the abstraction point for this is nacl-core, not each individual skill.
Next review: next NaCl release that gives the Codex channel a scripted graph-remediation path (e.g. ships graph-doctor.mjs through skills-for-codex and documents Bash-capable Codex hosts).
