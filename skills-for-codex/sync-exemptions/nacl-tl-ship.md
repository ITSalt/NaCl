Source root: fix/release-gate-fix-prs (Step-3 COMMIT gains the `Fix-level:`/`Fix-decision:` commit-body trailer, propagated when ship authors a fix commit); earlier: feat/skill-tools-pilot (slug + base-branch guard extracted into nacl-tl-ship/scripts/branch.sh as the single authority)
Intentional divergence: The Codex variant is a condensed, principle-level version that states the base-branch safety rule and branch naming as behavioural instructions ("in feature-branch strategy, do not commit to the base branch; if the current branch is wrong, stop and ask") rather than reproducing the procedural `tr | sed | cut` slug pipeline or the Step 2.5 guard bash block. It likewise does not reproduce the `Fix-level:`/`Fix-decision:` commit-trailer mechanics (the deterministic link the type-aware release gate reads) — the trailer is primarily authored by nacl-tl-fix Phase B; ship only propagates it when it authors the commit. The new bundled helper `scripts/branch.sh` (the root-side single authority, equivalence-pinned by `scripts/branch.test.sh`) is not part of the Codex distribution, so the root delegation has no Codex counterpart to mirror.
Next review: 2.20.0
