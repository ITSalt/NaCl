# NaCl packaged resources

This directory is the cache-contained resource root for the generated Codex
plugin. Public entry skills live at the plugin root; internal workflows,
references, graph assets, and current source documentation live below this
directory and are accessed only through package-relative paths.

The source repository README is intentionally not copied here because its
checkout-relative navigation is not valid inside an installed plugin cache.
