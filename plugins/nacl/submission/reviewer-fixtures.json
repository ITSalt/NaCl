{
  "$schema": "./reviewer-fixtures.schema.json",
  "schemaVersion": 2,
  "fixtureSetVersion": "nacl-skills-only-reviewer-fixtures-v1",
  "status": "LOCAL_CONTRACT_ONLY",
  "submissionType": "SKILLS_ONLY",
  "executionEvidence": {
    "localSimulation": {
      "status": "VERIFIED_CONTRACT_ONLY",
      "scope": "Bundle closure, bootstrap routing, confirmations, secret-free MCP configuration, and closed failure shapes are checked locally."
    },
    "liveReviewer": {
      "status": "NOT_RUN",
      "reason": "The exact Skills-only portal artifact and clean reviewer-machine run have not yet been submitted or executed."
    }
  },
  "positive": [
    {
      "id": "P1",
      "prompt": "After installing NaCl Skills only, inspect this project and explain the local initialization plan without changing anything.",
      "entrySkill": "nacl-init",
      "precondition": "No project MCP exists.",
      "localEvidence": { "status": "VERIFIED_CONTRACT_ONLY", "test": "tests/codex-plugin/scripts/nacl-skills-only-bundle.test.mjs", "scope": "Preflight and no-mutation contract; no live portal invocation." },
      "expectedBehavior": ["Resolve only the active skill bundle and explicit project root.", "Return a dry-run plan with NOT_RUN and make no file, Docker, or network mutation."],
      "expectedStatus": "NOT_RUN"
    },
    {
      "id": "P2",
      "prompt": "Initialize this project as demo-alpha after I provide the exact INIT_LOCAL_GRAPH:demo-alpha confirmation.",
      "entrySkill": "nacl-init",
      "precondition": "Docker is available, loopback ports are free, and the exact confirmation is present.",
      "localEvidence": { "status": "VERIFIED", "test": "tests/codex-plugin/scripts/nacl-skills-only-bootstrap-docker-e2e.test.mjs", "scope": "Exact staged-tree Docker bootstrap completed locally; hosted execution remains a required release gate." },
      "expectedBehavior": ["Run the bundle-relative OS bootstrap with no secret argument.", "Create copy-only project graph assets, a per-project container and volume, and a secret-free managed section in project .codex/config.toml."],
      "expectedStatus": "VERIFIED"
    },
    {
      "id": "P3",
      "prompt": "Verify the initialized NaCl project from a new task using one read canary.",
      "entrySkill": "nacl-diagnose",
      "precondition": "A new task has loaded the project-local neo4j MCP generated by nacl-init.",
      "localEvidence": { "status": "VERIFIED", "test": "tests/codex-plugin/scripts/nacl-skills-only-bootstrap-docker-e2e.test.mjs", "scope": "A separate local process completed MCP initialize, tools/list, and read-cypher canary; hosted execution remains required." },
      "expectedBehavior": ["Use only the project nacl_neo4j MCP.", "Return graph-backed evidence without invoking a package installation doctor or public endpoint."],
      "expectedStatus": "VERIFIED"
    },
    {
      "id": "P4",
      "prompt": "Plan a business-analysis context update for this initialized project and show the graph write confirmation first.",
      "entrySkill": "nacl-ba",
      "precondition": "Project MCP and read canary are verified.",
      "localEvidence": { "status": "VERIFIED_CONTRACT_ONLY", "test": "tests/codex-plugin/scripts/nacl-skills-only-bundle.test.mjs", "scope": "Workflow closure and approval contract only; no BA mutation." },
      "expectedBehavior": ["Select one packaged BA leaf and present the parameterized write plan.", "Stop before mutation until the BA approval required by that workflow is supplied."],
      "expectedStatus": "NOT_RUN"
    },
    {
      "id": "P5",
      "prompt": "Re-run nacl-init for this already initialized project without replacing existing files or secrets.",
      "entrySkill": "nacl-init",
      "precondition": "The existing graph environment, launcher, MCP entry, project identity, and ports match the requested binding.",
      "localEvidence": { "status": "VERIFIED", "test": "tests/codex-plugin/scripts/nacl-skills-only-bootstrap-docker-e2e.test.mjs", "scope": "The same exact-tree bootstrap reused the protected environment, binary receipt, container, and MCP binding locally." },
      "expectedBehavior": ["Reuse byte-identical assets and the existing protected secret.", "Read back the same secret-free MCP binding and report an idempotent verified result."],
      "expectedStatus": "VERIFIED"
    }
  ],
  "negative": [
    {
      "id": "N1",
      "scenario": "Invoke a non-init NaCl entry before the project MCP exists.",
      "entrySkill": "nacl-tl",
      "localEvidence": { "status": "VERIFIED_CONTRACT_ONLY", "test": "scripts/check-skills-only-bundle.mjs", "scope": "Public entry gate and forbidden package-tool scan." },
      "expectedBehavior": ["Do not call an absent package or project tool.", "Return BLOCKED/PROJECT_MCP_NOT_CONFIGURED and route to nacl-init."],
      "expectedStatus": "BLOCKED",
      "expectedCode": "PROJECT_MCP_NOT_CONFIGURED"
    },
    {
      "id": "N2",
      "scenario": "Ask nacl-init to mutate the project without the exact project-bound confirmation.",
      "entrySkill": "nacl-init",
      "localEvidence": { "status": "VERIFIED", "test": "tests/codex-plugin/scripts/nacl-skills-only-bundle.test.mjs", "scope": "Exact missing-confirmation path executes before project, Docker, or download mutation." },
      "expectedBehavior": ["Make no file, secret, Docker, registry, or network mutation.", "Return BLOCKED/CONFIRMATION_REQUIRED."],
      "expectedStatus": "BLOCKED",
      "expectedCode": "CONFIRMATION_REQUIRED"
    },
    {
      "id": "N3",
      "scenario": "Initialize or launch after malformed/conflicting TOML, a tampered binary or receipt, a project cache injection, an unsafe protected env, or a differing copied launcher.",
      "entrySkill": "nacl-init",
      "localEvidence": { "status": "VERIFIED", "test": "tests/codex-plugin/scripts/nacl-skills-only-bundle.test.mjs", "scope": "Real TOML parser adversarial cases plus binary, receipt, cache, symlink, and launcher checks execute locally." },
      "expectedBehavior": ["Fail closed before replacing the conflicting artifact or spawning the binary.", "Preserve all existing bytes and report the exact conflict, checksum, receipt, cache, or unsafe-file code."],
      "expectedStatus": "BLOCKED",
      "expectedCode": "TAMPER_OR_UNSAFE_STATE",
      "expectedCodes": ["CODEX_CONFIG_MALFORMED", "CODEX_MCP_CONFIG_CONFLICT", "BINARY_DIGEST_MISMATCH", "BINARY_RECEIPT_MISMATCH", "UNTRUSTED_BINARY_CACHE_PRESENT", "BINARY_RECEIPT_UNSAFE"]
    }
  ]
}
